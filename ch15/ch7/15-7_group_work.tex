\documentclass[12pt]{exam}

\newcommand{\course}{MTH 234 Summer 2021}
\newcommand{\qdate}{15.7 Triple Integrals} %PUT DATE HERE
\newcommand{\quiz}{Group Work} 

\input{../../common/preamble.tex}
\newcommand{\bif}{\quad\iff\quad}

%\printanswers
\noprintanswers

\begin{document}

\section*{\qdate}

%\subsection*{Template}

\begin{questions}

\question Evaluate the given triple integrals
\begin{parts}
    \part \(\int_{0}^{3}\int_{0}^{x}\int_{x-y}^{x+y}~y~dz~dy~dx\) 
        \ifprintanswers
        \begin{solution}
            \begin{align*}
                \int_{0}^{3}\int_{0}^{x}\int_{x-y}^{x+y}~y~dz~dy~dx & = \int_{0}^{3}\int_{0}^{x} yz|_{x-y}^{x+y}~dy~dx\\
                    &= \int_{0}^{3}\int_{0}^{x} y\left(x+y-(x-y)\right)~dy~dx\\
                    &= \int_{0}^3 \int_0^x\left(2y^3\right) dy~dx
            \end{align*}
        \end{solution}
    \else
        \vfill
    \fi
    \part \(\iiint_T x^2~dV\) where \(T\) is the tetrahedron with vertices
    \[
        (0,0,0), (1,0,0), (0,1,0), ~\text{and}~(0,0,1).
    \]
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi
    \part \(\iiint_{E} 6xy~dV\) where \(E\) is the region under the plane \(z=1+x+y\) and above the part of the \(xy\)-plane bounded by curves 
    \(y=\sqrt{x}\), \(y=0\), and \(x=1\).
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi 
    \end{parts}
\newpage

\question Use a triple integral to find the volume of the given solid
    \begin{parts}
    \part The solid enclosed by the cylinder \(y=x^2\) and the planes \(z=0\) and \(y+z=1\).
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi
    \part The tetrahedron enclosed by the coordinate planes \(x=0\), \(y=0\), \(z=0\) and the plane \(2x+y+z=4\).
        
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi


\end{parts}



\end{questions}

\end{document}

% soln : Question environment
    \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi