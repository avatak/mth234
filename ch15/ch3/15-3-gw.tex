\documentclass[12pt]{exam}

\newcommand{\course}{MTH 234 Summer 2021}
\newcommand{\qdate}{15.3 Double Integrals over General Regions} %PUT DATE HERE
\newcommand{\quiz}{Group Work} 

\input{../../common/preamble.tex}
\newcommand{\bif}{\quad\iff\quad}

%\printanswers
\noprintanswers

\begin{document}

\section*{\qdate}

%\subsection*{Template}

\begin{questions}

\question Evaulate the iterated and double integrals
\begin{parts}
    \part \(\int_0^1\int_{x^3}^{x}(x+2y)~dy~dx\)
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi
    \part \(\int_0^1\int_0^{s^2}\cos(s^3)~dt~ds\)
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi
    \part \(\iint_D y^2~dA\) with \(D=\{(x,y)~|~-1\le y \le 1, -y-2\le x \le y\}\)
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi
\end{parts}

\newpage

\question Evaulate \(\iint_D xy~dA\), where \(D\) is the region enclosed by \(y=x^2, y=3x\)
    \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi

\question Evaluate \(\iint_D y^2~dA\) where \(D\) is the triangular region with vertices \((0,1),(1,2),(4,1)\).
    \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi

\newpage

\question Find the volume of the solid under the plane \(x-2y+z=1\) and above the region bounded by \(x+y=1\) and \(x^2+y=1\).
    \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi 

\question Sketch the region of integration and rewrite the integral with the order of integration switched
\begin{parts}
    \part \(\int_0^1\int_0^y f(x,y)~dx~dy\)
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi
    \part \( \int_1^2\int_0^{\ln(x)}~f(x,y)~dy~dx \)
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi
\end{parts}

\newpage

\question Evaluate each integral by changing the order of integration
\begin{parts}
    \part \(\int_0^4\int_{\sqrt{x}}^{2}\dfrac{1}{y^3+1}~dy~dx\)
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi

    \part \(\int_0^1\int_{3y}^3 e^{x^2}~dx~dy\)
        \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi
\end{parts}

\question Find the average value of \(f(x,y)=x\sin (y)\) over the region bounded by the curves \(y=0\), \(y=x^2\), and \(x=1\).
    \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi


\end{questions}

\end{document}

% soln : Question environment
    \ifprintanswers
        \begin{solution}
        \end{solution}
    \else
        \vfill
    \fi